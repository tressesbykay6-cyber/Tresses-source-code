rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Browser uploads are disabled. Media is accepted, validated, transformed,
    // and published only by the server-side admin endpoint.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
